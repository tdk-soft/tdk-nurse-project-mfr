apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  labels:
    app: mfr-care
data:
  NODE_ENV: production # Will be overridden per environment
  LOG_LEVEL: info
  API_VERSION: v1
  CORS_ORIGIN: '*'
  RATE_LIMIT_WINDOW_MS: '60000'
  RATE_LIMIT_MAX_REQUESTS: '100'
